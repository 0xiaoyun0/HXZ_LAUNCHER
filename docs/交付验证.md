# 交付验证

## 0.4.0

蓝图、论坛、聊天位置与焦点、社区通道Opus语音、20人限制与新版布局的针对性检查见 [0.4.0 开发与交付](0.4.0开发与交付.md)。客户端与服务端分别交付；公网服务升级由管理员执行，保留data和.env。

## 0.3.0

验证日期：2026-09-15。客户端9项、服务端3项针对性检查，类型与静态检查通过。真实26.2安装经历取消、暂存、继续并完成5057项资源；双桌面验证社区聊天、音频RTP、三服日志和头像。最终打包客户端验证固定实例、MOD启停、头图、简化首页、全局背景与任务详情。独立服务端发布包health返回0.3.0，网页后台与桌面来源WebSocket通过。ZIP检查不含运行数据、密码或配置。

公网现有0.2.2服务可握手；已登录公网长连接、实际玩家入服和正式用户环境NSIS跨版本覆盖未执行。管理员须独立升级公网社区服务，保留data和.env。历史四版发布文件完整归档，0.3.0公开附件按SHA-256核对。


## 0.2.2

验证日期：2026-09-14。独立服务端日志分组、重复地址拒绝、维护状态、重新绑定后的缓存隔离通过；头像只能更新本人，非 PNG 拒绝，广播使用小版本标识。实际随包 HXZ UP 验证无界面与独立窗口两种模式，Windows 进程存在 HXZ UP 窗口，showChangelog 开关正确，维护完成后退出。

打包的两个桌面窗口验证各服日志页只显示自己的内容、原生缩略头像选择与预览、保存后另一客户端能加载头像、聊天与实际语音 RTP、静音/离开，以及弹窗开关默认开启并能关闭。使用隔离测试身份和 HXZ UP 协议服务，未使用真实玩家凭据。实际三个服的接入地址尚未提供，未假设它们的映射。

源码类型、静态及格式检查通过。0.2.2 客户端与社区服务端分别打包，正式部署前需在独立后台配置三个更新日志来源。

## 0.2.1

验证日期：2026-09-14。桌面 file:// WebSocket 原先失败，同客户端修复后连接成功；旧 null 配置兼容且未知来源拒绝。双打包 Electron 页面验证公告、聊天、实际音频 RTP、静音/离开。版本同步 0.2.1。

新增进度检查覆盖实际分块下载、坏源回退、校验失败不递增完成数、Java 安装组件的真实下载计数、用户真实 mrpack 解压和 HXZ UP 识别、缓存 NeoForge 启动准备。任务面板用真实进度事件验证搜索、失败阶段保留和关闭操作；浅色/深色语音区、文字标识、1024×680 大字号布局通过检查。源码类型、静态、格式检查通过；未重复做游戏入服或公网语音验收。

发布路径 release/0.2.1/client 与 release/0.2.1/server，客户端和服务端继续独立。

## 0.2.0

验证日期：2026-09-14。保留下面的 0.1.0 历史记录。

- Forge 重复 Gson 回归：修改前生成的实际 classpath 有 3 项但唯一文件只有 2 个，修改后通过。独立版本清单按 Maven 坐标合并，实际文件路径再次去重。
- 用户真实包 E:/QQDOWNLOAD/hxzv3.mrpack：完整执行 Minecraft 1.21.1 与 NeoForge 21.1.250 安装处理器、overrides 应用，识别并启用 HXZ UP，servers 去重为 3 个。测试安装在 .test/import-020，未覆盖用户原测试游戏。
- NeoForge 使用本机 Zulu Java 21，Forge 47.4.23 / 1.20.1 使用 Zulu Java 17，均启动并持续运行至 OpenAL、纹理图集与渲染初始化。Forge 真实安装发现 path:null，修复后完成所有处理器。测试使用隔离的本地身份，没有登录真实账号或连接正式服务器。
- 本机 Oracle Java 21.0.4 在 NeoForge 早期窗口处以 0xC0000005 退出；相同安装在 Zulu 21 正常渲染。加入专门的异常提示，不把此问题误报为重复依赖。
- 客户端类型、格式和静态检查通过；4 项核心检查通过。服务端 2 项检查覆盖可信皮肤站身份、社区权限、消息、语音信令隔离及独立网页后台登录、公告发布、管理员设置、密码更改、跨来源拒绝。
- 实际网页界面验证独立后台、导航、mrpack 提示与大字号布局；最终 Electron 0.2.0 程序验证原生文件拖入、HXZ UP 检测、真实在线 Minecraft 目录、字号保存、1024×680 窗口下 22 px 字号的选择框和启动按钮、启动器更新接口。
- 独立服务端发布包使用随包 Node.js 启动，/health 返回 hxz-community / 0.2.0，/admin/ 可访问。客户端与服务端发布目录分别为 release/0.2.0/client 和 server。
- 发布包含客户端安装程序、便携 ZIP、独立服务端 ZIP、latest.yml、blockmap 和 SHA256SUMS.txt；客户端资源中包括安装组件和自更新缓存配置，实际 electron-updater SDK 成功读取发布资源并创建下载缓存助手。

范围限制：没有真实玩家入服、公网 TURN 或正式 HTTPS 发布站跨版本覆盖升级验收。启动器更新需管理员部署发布目录；便携版不承诺原地安装式升级。服务端单独部署，不由客户端自动启动。

截图见 docs/screenshots/020-desktop-*.png 与 020-admin.png。真实安装日志与临时测试配置在 .test，不进入发布包。

## 0.1.0

验证日期：2026-09-14。

- 客户端 TypeScript 类型检查与 oxlint 静态检查通过。
- 核心验证：路径越界、版本继承、带空格的 JVM 参数、下载校验失败保留原文件、有限并发和大响应限制。
- 实际随包 HXZ UP 1.0.3 JAR 已通过无界面运行及维护模式联调；Windows 中文输出编码已修正。
- 社区服务验证了可信上游身份、管理员权限、公告、聊天历史、同房语音信令与封禁。
- 两个独立浏览器连接实际测试社区服务，消息互通；WebRTC 实际收到音频 RTP 数据；静音关闭发送轨道，离开关闭连接。测试只使用隔离的虚拟账号和模拟麦克风。
- 实际 Electron 构建已打开，验证桌面桥、Java 扫描、上下文隔离与沙箱。
- Windows x64 最终打包程序可正常打开，识别现有 hxzv3 / 1.21.1 / NeoForge 21.1.250；已验证实例页面与后退，1024×680 最小窗口下启动按钮仍可见；实际 Electron 包的麦克风权限处理通过模拟设备验证，不访问真实麦克风。
- 对现有测试整合包执行了真实依赖/资源校验与参数组装，没有使用玩家账号启动游戏或连接生产游戏服务器。
- 附带 Node.js 24 的社区服务端运行成功，/health 返回 0.1.0。

交付：Windows x64 安装程序、便携版 ZIP、Windows x64 社区服务端 ZIP、源代码、部署说明与 SHA-256 校验文件。

仍需实际配置/验收：生产社区域名、各实例 HXZ UP 客户端地址、管理员角色 UUID、TURN 服务；真实玩家登录和入服、公网语音质量。皮肤站网页首次登录仍独立进行，后续保存网页会话；游戏与社区使用统一皮肤站身份。

本次未推送 GitHub，也未部署到外部主机。
