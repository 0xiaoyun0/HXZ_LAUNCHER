# 0.2.0 当前任务

用户要求：修复 Forge/NeoForge Duplicate key Gson；补全 MC 版本/加载器/整合包下载安装；整体字号可调且默认增大、按钮明确；UI 颜色/背景/布局；国内镜像优先并回退；社区连接修复、独立网页管理；Electron 自更新。拖入 mrpack 自动配置，识别有效 HXZ UP 配置并启用更新。

E:/QQDOWNLOAD/hxzv3.mrpack 已检查：MC 1.21.1、NeoForge 21.1.250；11 ZIP项、files=[]；client-overrides/updater 含 config.json、launcher-agent.jar、updater-1.0.2.jar。servers 有有效 HTTPS 地址且重复一项。必须用真实包导入验证。

最新边界：服务在本地，但用户明确客户端和服务端不要堆在一起。分别运行/打包；客户端不内置、不启动社区服务；网页管理属于 server。仅曾提议本地启动入口，没有实现，禁止继续该方案。

已使用 diagnosing-bugs 技能。client/test/forge-regression.test.mjs 改前失败，实际 prepareLaunch 生成3项CP而唯一文件2项。独立JSON按Maven坐标去重后通过。仍需检查 realpath CP去重替换是否命中，并真实运行 Forge/NeoForge。

已新增 core/sources.mjs BMCLAPI/MCIM 镜像；io.mjs GET元数据和下载加镜像优先回退，GET允许镜像302，认证POST不跟随重定向。

已建立 client/installer-java/up/hxz/{LauncherInstall,GameInstaller}.java。GameInstaller 来自同工作区 HXZUP（非HMCL/PCL代码），独立拷贝修改镜像优先；编译资源 resources/installer/game-installer.jar。类路径此JAR在先、resources/hxzup/updater-1.0.3.jar在后。CLI参数 updater目录/profile.json/config.json，安装并发布新实例JSON/JAR。只在新建或本程序标记的失败安装目录中调用。

以上为早期工作记录，下方为最终交付状态。

环境：Node E:/node.js/node.exe；JDK21 C:/Program Files/Java/jdk-21；Electron44 已安装，Electron RUN_AS_NODE内Node24支持node:sqlite。但不要把server嵌入client。真实测试游戏E:/003更新/测试游戏/.minecraft/versions/hxzv3。截图F:/PCLLL不在本机。保留release/0.1.0。所有原MD在本轮已完整重读；压缩后重新阅读所有项目MD。

最新进度：mrpack/目录/安装、两个新页面与全局拖入、字号/背景/布局、独立 admin 网页、连接检查、自更新已编写。真实 hxzv3.mrpack 安装和 HXZ UP 自动识别成功（3个去重地址）。NeoForge21.1.250/1.21.1 使用 Zulu21 成功渲染，Oracle21.0.4 出现0xC0000005已添加提示。Forge47.4.23/1.20.1 安装发现path:null，新增ForgeInstaller.java修复后Zulu17成功安装渲染。核心4项、服务权限和独立后台、实际UI大字号导航与mrpack识别通过。首次0.2.0构建成功；其后补了按游戏元数据选择Java和并发任务锁，需最终重新构建、分开打包以及实际Electron验证。package-release.mjs已编写但未执行。

最终状态：0.2.0 源码和两个独立发布目录完成。最终 Electron 原生拖入、HXZ UP 检测、在线目录、字号保存和最小窗口布局通过。发现 electron-updater 下载需要缓存配置，已加入 resources/app-update.yml，重新构建并用实际 SDK 验证缓存助手。安装程序、便携包、服务端包、latest.yml、blockmap、SHA256SUMS 已重新生成。独立服务端发布包 /health 和 /admin/ 通过，正式本地服务另行在8787运行，客户端不负责启动它。没有真实账号入服、正式更新站跨版本升级、公网TURN验收或GitHub推送。
