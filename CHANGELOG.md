# 更新日志

## 0.4.2

- 修复多格式整合包导入失败，补齐 CurseForge、Prism/MultiMC、普通 ZIP 的元数据解析、实例文件提取与 HXZ UP 识别。
- 在线 MOD 按游戏版本和加载器选择兼容文件、安装必需依赖；增加下载校验和失败清理。
- 补齐评论点赞、嵌套回复持久化及跨页回复上下文，修复蓝图库版本筛选接口。
- 补齐语音频道进出提示音事件，避免按键说话干扰文本输入和改键操作。
- 保留升级前的个性化设置，栏目透明度不再影响文字；隐藏侧栏后可恢复设置，实例可单独指定内存策略。
- 内嵌隐藏活动页面，玩家进度按角色保存，社区后台可管理活动内容与开放时间。
- 补齐遗漏的颜文字组件，修复可复现构建流程；启动器日志支持分页读取历史版本。

## 0.4.1

- 优化桌面端整体布局，恢复 0.4.0 默认浅色外观与页面尺寸比例；修复侧栏、启动栏、默认服务器收起栏、实例操作区、语音房间、表情窗口和个性化页面的错位、拥挤及间距问题。
- 新增默认服务器与下拉菜单的平滑收起/展开动画；实例支持收藏置顶、截图/版本/存档目录入口，整合包导入支持 mrpack、CurseForge、Prism、MultiMC 与 ZIP。
- 语音聊天支持可自定义按键说话，默认 T 键并显示按键说话状态反馈；保留全程说话选项及频道进出提示音。
- 修复论坛点赞数量显示为 undefined、回复层级错误的问题；回复按评论层级展示，更新日志页完整读取启动器历史更新内容。
- 修复机械动力蓝图库游戏版本筛选无内容、空白下拉选项以及颜文字显示不全的问题；颜文字与 Emoji 窗口统一尺寸，颜文字每行最多三项。
- 设置页新增自动内存分配、语音麦克风选项和受限个性化配置；恢复默认外观时保留必要栏目，默认底部栏名称为“任务详情”。

## 0.4.0

- 新增机械动力蓝图库：结构 NBT 上传、封面、分类与版本筛选、审核后公开及下载。管理员可在后台任命或撤销蓝图审核员。
- 新增幻想镇论坛：玩家发帖、回复、点赞；后台支持置顶、锁帖、隐藏、删除帖子及管理回复。
- 修复返回聊天大厅跳到最早消息、连续发送后输入框失去焦点的问题，保留草稿和阅读位置。
- 语音通过现有社区连接转发 Opus 音频，无需单独配置语音服务；房间上限20人，悬停查看房间成员。
- 改善语音房间、麦克风选择和提示文字间距，修复切换页面后设备选项消失、大字号下区域重叠。
- 删除左上角前进、后退按钮；新增页面沿用现有深浅主题、字号与桌面布局。
- 客户端与社区服务端独立发布；使用新功能需将社区服务端一并升级至0.4.0，保留原data与.env。

## 0.3.4

- 启动器更新支持 GitHub、GHProxy、GHFast、GH-Proxy 多个来源，检查超时自动跳过，下载中断或长时间无进度时自动换源。
- 更新检查优先采用可访问来源中的最高正式版本，下载始终锁定该版本；镜像缓存旧版不会覆盖已获取的新版。
- 新增发布信息签名验证，保留安装文件 SHA-512 校验，拒绝错误网页或被篡改的更新信息。
- GitHub 更新日志不可访问时，从已验证的镜像发布信息读取最新更新说明。
- 保留默认自动更新、设置关闭开关和等待游戏/安装任务结束后自动安装的行为。

## 0.3.3

- 修复设置页外观区域开关与个性化按钮紧贴、字号放大时布局拥挤的问题。
- 开关与个性化按钮固定上下分行，保留垂直间距，主题卡片支持窄区域换行；移除用于撑间距的空段落。

## 0.3.2

- 设置新增8、16、32、64、128五档下载并发，默认64，旧配置自动使用默认值。
- 游戏安装、启动前资源补全、整合包文件及Java运行环境下载使用所选并发，下一次开始或继续任务时生效。
- 统一全部动态、三个服和启动器更新的公告页签样式；修复启动器更新页签刷新与后退状态。
- 修正设置页多余字符，保留0.3.1下载线程退出修复。

## 0.3.1

- 修复某下载源卡住时，即使其他源已完成也会抛出 `Download workers did not exit`、导致整个安装暂停的问题。
- 单个文件按国内镜像优先、失败再换源，多个文件仍保持有界并行；保留校验、临时失败重试和取消清理。
- 已失败的安装可继续，复用已校验资源；无需删除游戏目录。
- 服务端功能无变化，仅同步发布版本号。

## 0.3.0

- 修复公网社区请求失败后不再连接的问题；增加持续退避重连、网络恢复、会话刷新与心跳，减少后台窗口影响。
- 从 HXZ_LAUNCHER 的 GitHub Releases 获取启动器更新日志；默认检查、下载并静默安装更高正式版本，可关闭自动更新，游戏和安装任务结束后才重启。
- 整合包安装使用独立暂存目录，失败可在任务详情继续或清理；保留已校验文件，修复临时网络故障直接终止、下载超时与文件句柄占用。
- 安装器后台执行，安装确认框不再挡住日志；任务详情可随时展开/收起。运行日志统一到任务详情。
- 新增每实例 MOD 管理与自动进入服务器设置。
- 固定原版生存群组服（26.2 / s1.hxzmc.top），模组一服、二服保留待发布占位。
- 改善全窗口背景展示，新增实例自定义头图与简化启动首页；分服公告入口集中到通知公告。
- 客户端、独立社区服务端和历史版本分别发布。

## 0.2.2

三个服独立 HXZ UP 更新日志、按角色保存的自定义头像、默认开启的 HXZ UP 弹窗开关、加宽语音房间与横排人数。

## 0.2.1

修复打包桌面的社区来源识别；优化语音区域与品牌标识；安装及启动显示真实阶段进度，新增可搜索任务详情。

## 0.2.0

Forge/NeoForge 依赖修复；Minecraft、加载器与整合包下载；拖入 mrpack，识别 HXZ UP；字号、颜色、背景与布局；国内镜像；独立社区网页管理及启动器更新基础。

## 0.1.0

桌面启动器、实例扫描与启动、皮肤站账号、HXZ UP、三个公告组、公共聊天与语音、独立社区服务。
